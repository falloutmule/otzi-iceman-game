{
  "name": "otzi-iceman-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build-single-file.mjs",
    "serve": "python -m http.server 8099",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
